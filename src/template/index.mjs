import { default as single } from './single.mjs';
import { default as random } from './random.mjs';
import { default as multiple } from './multiple.mjs';

export default {
    single: single,
    random: random,
    multiple: multiple,
};
